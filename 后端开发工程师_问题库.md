# 后端开发工程师问题库

> 难度系数说明：
> - 0-0.3：基础概念
> - 0.3-0.6：原理探究
> - 0.6-0.9：场景应用
> - 0.9-1.0：极限深挖

---

## 一、Java基础

### 1.1 面向对象编程

#### 问题1：什么是面向对象编程的三大特性？
- **难度系数**：0.2
- **追问深度**：基础概念
- **参考答案**：封装、继承、多态。封装是将数据和操作绑定并隐藏实现细节；继承是子类继承父类特性实现代码复用；多态是同一操作作用于不同对象产生不同行为。
- **评分关键词**：封装、继承、多态、代码复用、隐藏细节

#### 问题2：Java中方法重载和方法重写有什么区别？
- **难度系数**：0.25
- **追问深度**：基础概念
- **参考答案**：重载（Overload）在同一类中，方法名相同、参数列表不同，编译时多态；重写（Override）在子类中，方法名、参数、返回值相同，运行时多态。
- **评分关键词**：重载、重写、参数列表、编译时、运行时、子类

#### 问题3：接口和抽象类有什么区别？
- **难度系数**：0.3
- **追问深度**：原理探究
- **参考答案**：接口只能有抽象方法（JDK8+可有默认方法），多实现，强调行为规范；抽象类可有成员变量和具体方法，单继承，强调代码复用。接口是"has-a"，抽象类是"is-a"。
- **评分关键词**：多实现、单继承、默认方法、行为规范、代码复用

#### 问题4：你能举个例子说明多态在实际开发中的应用吗？
- **难度系数**：0.25
- **追问深度**：基础概念
- **参考答案**：例如支付系统，定义Payment接口，有pay()方法。Alipay、WechatPay、UnionPay都实现Payment接口。业务代码使用Payment类型接收，根据实际对象调用不同支付方式，便于扩展新支付方式。
- **评分关键词**：接口、实现类、扩展性、解耦、运行时绑定

#### 问题5：为什么Java不支持多继承？如果线程数超过CPU核心数，上下文调度具体怎么实现的？
- **难度系数**：0.95
- **追问深度**：极限深挖
- **参考答案**：Java不支持多继承主要是避免菱形继承问题（钻石问题），即多个父类有相同方法时产生歧义。线程调度由操作系统内核实现，通过时间片轮转或优先级调度。当线程数超过CPU核心数时，操作系统通过上下文切换（保存/恢复寄存器状态）让多个线程分时复用CPU。JVM的线程与操作系统线程一一对应（1:1模型），调度完全由内核控制。
- **评分关键词**：菱形继承、歧义、时间片、上下文切换、内核调度、1:1模型

---

### 1.2 集合框架

#### 问题6：ArrayList和LinkedList有什么区别？
- **难度系数**：0.2
- **追问深度**：基础概念
- **参考答案**：ArrayList基于动态数组，查询快O(1)、增删慢O(n)；LinkedList基于双向链表，查询慢O(n)、增删快O(1)。ArrayList适合随机访问，LinkedList适合频繁增删。
- **评分关键词**：数组、链表、查询、增删、时间复杂度

#### 问题7：HashMap的底层原理是什么？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：JDK8之前是数组+链表，JDK8及之后是数组+链表+红黑树。通过hash计算索引位置，冲突时用链表存储，链表长度超过8转为红黑树，低于6转回链表。默认容量16，负载因子0.75。
- **评分关键词**：数组、链表、红黑树、hash、冲突、扩容

#### 问题8：为什么HashMap的链表长度超过8要转为红黑树？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：链表查询时间复杂度O(n)，红黑树O(log n)。当冲突严重时链表过长影响性能，转为红黑树提升查询效率。选择8是因为根据泊松分布，hash冲突达到8的概率已经很低（0.00000606），避免频繁转换的开销。
- **评分关键词**：时间复杂度、泊松分布、查询效率、冲突概率

#### 问题9：ConcurrentHashMap是如何实现线程安全的？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：JDK7使用分段锁（Segment），每个Segment继承ReentrantLock，锁粒度为段。JDK8使用CAS+synchronized，锁粒度为桶（链表头节点）。读操作无锁（volatile保证可见性），写操作只锁当前桶。
- **评分关键词**：分段锁、CAS、synchronized、锁粒度、volatile

#### 问题10：高并发场景下你会怎么选择集合类？
- **难度系数**：0.7
- **追问深度**：场景应用
- **参考答案**：读多写少用CopyOnWriteArrayList/CopyOnWriteArraySet；高并发读写用ConcurrentHashMap；需要排序用ConcurrentSkipListMap；阻塞队列用LinkedBlockingQueue或ArrayBlockingQueue。根据具体场景选择，避免使用Collections.synchronizedXXX包装器，性能较差。
- **评分关键词**：CopyOnWrite、ConcurrentHashMap、阻塞队列、读写比例、性能

---

### 1.3 异常处理

#### 问题11：Java中的异常分类有哪些？
- **难度系数**：0.2
- **追问深度**：基础概念
- **参考答案**：Throwable分为Error和Exception。Error是系统级错误，如OutOfMemoryError。Exception分为受检异常（Checked，编译时处理）和非受检异常（Unchecked，运行时异常）。
- **评分关键词**：Error、Exception、受检异常、非受检异常、运行时

#### 问题12：try-catch-finally中，如果catch和finally都有return，会返回哪个？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：finally中的return会覆盖catch中的return。但如果在finally中修改return的引用类型变量，不会影响返回值（因为return值已确定）。建议在finally中不要写return。
- **评分关键词**：覆盖、引用类型、返回值、执行顺序

---

### 1.4 多线程与并发

#### 问题13：创建线程有哪些方式？
- **难度系数**：0.2
- **追问深度**：基础概念
- **参考答案**：1)继承Thread类；2)实现Runnable接口；3)实现Callable接口（有返回值）；4)使用线程池Executor框架。推荐使用后两种，避免单继承限制，支持返回值和线程复用。
- **评分关键词**：Thread、Runnable、Callable、线程池、返回值

#### 问题14：synchronized和ReentrantLock有什么区别？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：synchronized是JVM层面，自动获取/释放锁，不可中断，非公平锁；ReentrantLock是API层面，需手动释放，可中断、可超时、可公平，支持条件变量。ReentrantLock更灵活但使用复杂。
- **评分关键词**：JVM层面、可中断、公平锁、条件变量、手动释放

#### 问题15：volatile关键字的作用是什么？能保证线程安全吗？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：volatile保证可见性（修改立即可见）和禁止指令重排序。但不能保证原子性，多线程自增操作仍会有问题。适合状态标记、双重检查锁等场景，不适合复合操作。
- **评分关键词**：可见性、指令重排序、原子性、内存屏障、状态标记

#### 问题16：什么是CAS？有什么优缺点？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：CAS（Compare And Swap）是比较并交换，使用预期值和内存值比较，相等则更新。优点是无需加锁，性能高；缺点是存在ABA问题、自旋消耗CPU、只能保证单个变量原子性。
- **评分关键词**：比较并交换、ABA问题、自旋、原子性、Unsafe

#### 问题17：线程池的核心参数有哪些？
- **难度系数**：0.35
- **追问深度**：原理探究
- **参考答案**：corePoolSize（核心线程数）、maximumPoolSize（最大线程数）、keepAliveTime（非核心线程存活时间）、workQueue（任务队列）、threadFactory（线程工厂）、handler（拒绝策略）。
- **评分关键词**：核心线程、最大线程、队列、拒绝策略、存活时间

#### 问题18：线程池的拒绝策略有哪些？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：AbortPolicy（抛异常，默认）、CallerRunsPolicy（调用线程执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢弃最老任务）。可根据业务需求选择或自定义。
- **评分关键词**：AbortPolicy、CallerRunsPolicy、Discard、自定义

#### 问题19：如何合理设置线程池大小？
- **难度系数**：0.65
- **追问深度**：场景应用
- **参考答案**：CPU密集型：核心数+1；IO密集型：核心数*2或核心数/(1-阻塞系数)。实际应根据压测调整，考虑任务特性、QPS、响应时间要求。使用动态线程池（如美团DynamicTp）可在线调整。
- **评分关键词**：CPU密集型、IO密集型、核心数、压测、动态调整

#### 问题20：高并发场景下你会怎么选择进程/线程模型？
- **难度系数**：0.75
- **追问深度**：场景应用
- **参考答案**：IO密集型用多线程（如Web服务），CPU密集型用线程数≈CPU核心数。Node.js用单线程+事件循环，Go用协程（Goroutine）更轻量。Java可用CompletableFuture或响应式编程（WebFlux）提升并发。Netty使用主从Reactor多线程模型，主线程接收连接，工作线程处理IO。
- **评分关键词**：IO密集、CPU密集、协程、事件循环、Reactor、CompletableFuture

---

## 二、JVM

### 2.1 JVM内存结构

#### 问题21：JVM内存结构有哪些区域？
- **难度系数**：0.25
- **追问深度**：基础概念
- **参考答案**：堆（对象实例）、方法区（类信息、常量）、虚拟机栈（栈帧、局部变量）、本地方法栈（Native方法）、程序计数器（字节码行号）。堆和方法区线程共享，其他线程私有。
- **评分关键词**：堆、方法区、栈、程序计数器、线程共享

#### 问题22：堆为什么要分新生代和老年代？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：根据分代收集理论，大部分对象朝生夕死。新生代用复制算法（Eden:S0:S1=8:1:1），老年代用标记-清除/整理。分开后可针对不同代特点选择合适算法，提升GC效率。
- **评分关键词**：分代收集、复制算法、朝生夕死、GC效率

#### 问题23：什么是内存泄漏？如何排查？
- **难度系数**：0.6
- **追问深度**：场景应用
- **参考答案**：内存泄漏是对象不再使用但无法被GC回收，导致内存持续增长。排查：1)使用jmap生成dump文件；2)MAT或VisualVM分析大对象；3)查看GC日志；4)检查静态集合、未关闭资源、监听器未移除等常见原因。
- **评分关键词**：dump、MAT、GC日志、静态集合、未关闭资源

---

### 2.2 垃圾回收

#### 问题24：如何判断对象是否可以被回收？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：引用计数法（有循环引用问题）和可达性分析（从GC Roots出发不可达）。GC Roots包括虚拟机栈引用、方法区静态属性、本地方法引用等。
- **评分关键词**：引用计数、可达性分析、GC Roots、循环引用

#### 问题25：垃圾回收算法有哪些？
- **难度系数**：0.35
- **追问深度**：原理探究
- **参考答案**：标记-清除（产生碎片）、复制算法（浪费空间）、标记-整理（移动对象）、分代收集（新生代复制、老年代标记整理）。
- **评分关键词**：标记清除、复制、标记整理、分代收集、内存碎片

#### 问题26：CMS收集器的工作原理是什么？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：初始标记（STW，标记GC Roots直接关联）→ 并发标记（遍历对象图）→ 重新标记（STW，修正并发期间变化）→ 并发清除。优点是低停顿，缺点是CPU敏感、浮动垃圾、内存碎片。
- **评分关键词**：初始标记、并发标记、重新标记、STW、浮动垃圾

#### 问题27：G1收集器有什么特点？
- **难度系数**：0.6
- **追问深度**：原理探究
- **参考答案**：G1将堆划分为多个Region，优先回收价值最大的Region。可预测停顿时间，整体是标记-整理，局部是复制算法，无内存碎片。适合大堆（6G以上）。
- **评分关键词**：Region、可预测停顿、标记整理、大堆

#### 问题28：如果线程数超过CPU核心数，上下文调度具体怎么实现的？
- **难度系数**：0.95
- **追问深度**：极限深挖
- **参考答案**：操作系统通过进程控制块（PCB）保存线程上下文（寄存器、程序计数器、栈指针等）。当时间片用完或阻塞时，内核进行上下文切换：保存当前线程状态到PCB，从就绪队列选下一个线程，恢复其上下文。JVM线程是内核线程（1:1模型），切换开销约1-10微秒，包括用户态到内核态转换。
- **评分关键词**：PCB、上下文切换、时间片、内核态、1:1模型、寄存器

---

### 2.3 类加载机制

#### 问题29：类加载的过程是怎样的？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：加载（读取.class文件）→ 验证（字节码安全）→ 准备（分配内存设默认值）→ 解析（符号引用转直接引用）→ 初始化（执行<clinit>()）。
- **评分关键词**：加载、验证、准备、解析、初始化、clinit

#### 问题30：什么是双亲委派模型？为什么要用？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：类加载器收到加载请求，先委派给父加载器，父加载器无法加载才自己加载。保证核心类库（如java.lang.String）由Bootstrap加载，防止自定义类替换核心类，保证安全性。
- **评分关键词**：委派、Bootstrap、安全性、核心类库、自定义

#### 问题31：如何打破双亲委派模型？
- **难度系数**：0.7
- **追问深度**：场景应用
- **参考答案**：1)重写loadClass()方法（Tomcat为隔离Web应用）；2)使用线程上下文类加载器（SPI机制，如JDBC）。OSGi、热部署等场景需要打破。
- **评分关键词**：loadClass、线程上下文类加载器、SPI、Tomcat、热部署

---

## 三、Spring框架

### 3.1 IOC容器

#### 问题32：什么是IOC和DI？
- **难度系数**：0.25
- **追问深度**：基础概念
- **参考答案**：IOC（控制反转）是将对象创建和依赖管理交给容器。DI（依赖注入）是IOC的实现方式，通过构造器、setter或字段注入依赖。降低耦合，提高可测试性。
- **评分关键词**：控制反转、依赖注入、容器、耦合、可测试性

#### 问题33：Spring Bean的生命周期是怎样的？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：实例化 → 属性赋值 → 初始化前（Aware接口） → 初始化（InitializingBean、@PostConstruct） → 初始化后（BeanPostProcessor） → 使用 → 销毁（DisposableBean、@PreDestroy）。
- **评分关键词**：实例化、Aware、InitializingBean、BeanPostProcessor、销毁

#### 问题34：Spring如何解决循环依赖？
- **难度系数**：0.6
- **追问深度**：原理探究
- **参考答案**：通过三级缓存解决setter/字段注入的循环依赖。一级缓存（成品Bean）、二级缓存（早期暴露Bean）、三级缓存（ObjectFactory生成代理）。构造器注入无法解决，会抛异常。
- **评分关键词**：三级缓存、早期暴露、ObjectFactory、构造器注入、代理

#### 问题35：为什么构造器注入不能解决循环依赖？
- **难度系数**：0.75
- **追问深度**：极限深挖
- **参考答案**：构造器注入时，对象需要完全实例化（调用构造方法）才能放入缓存。循环依赖时，A构造需要B，B构造需要A，但此时A还未实例化完成无法提供。而setter注入是先实例化（无参构造）再放入缓存，后续注入依赖。
- **评分关键词**：实例化完成、构造方法、缓存、无参构造、注入时机

---

### 3.2 AOP

#### 问题36：Spring AOP的实现原理是什么？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：基于动态代理。目标类实现接口用JDK动态代理（生成接口实现类），否则用CGLIB代理（生成子类）。通过ProxyFactory创建代理，拦截方法调用执行通知链。
- **评分关键词**：JDK动态代理、CGLIB、代理工厂、接口、子类

#### 问题37：JDK动态代理和CGLIB代理有什么区别？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：JDK代理需要实现接口，基于反射调用目标方法；CGLIB基于ASM字节码生成子类，通过FastClass机制直接调用，性能略高。JDK代理只能代理接口方法，CGLIB可代理类方法（不能代理final方法）。
- **评分关键词**：接口、反射、ASM、字节码、FastClass、final

#### 问题38：Spring事务的传播行为有哪些？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：REQUIRED（默认，加入当前事务）、REQUIRES_NEW（挂起当前事务，创建新事务）、NESTED（嵌套事务，savepoint）、SUPPORTS（有则加入，无则非事务）、NOT_SUPPORTED（挂起当前事务，非事务执行）、MANDATORY（必须有事务，否则抛异常）、NEVER（必须无事务，否则抛异常）。
- **评分关键词**：REQUIRED、REQUIRES_NEW、NESTED、挂起、savepoint

---

### 3.3 Spring Boot

#### 问题39：Spring Boot的自动配置原理是什么？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：通过@EnableAutoConfiguration启用，读取META-INF/spring.factories中的自动配置类，@Conditional条件注解判断是否生效，通过xxxProperties绑定配置。约定大于配置，简化开发。
- **评分关键词**：spring.factories、Conditional、Properties、约定大于配置

#### 问题40：Spring Boot Starter的作用是什么？
- **难度系数**：0.35
- **追问深度**：基础概念
- **参考答案**：Starter是预定义的依赖组合，包含相关jar包和自动配置类。使用时只需引入starter，无需手动配置。如spring-boot-starter-web包含web开发所需依赖。
- **评分关键词**：依赖组合、自动配置、简化、约定

---

## 四、数据库

### 4.1 MySQL

#### 问题41：MySQL的索引底层是什么数据结构？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：InnoDB使用B+树。非叶子节点存储键值和指针，叶子节点存储完整数据并按顺序链接。支持范围查询和顺序访问，减少IO次数。
- **评分关键词**：B+树、非叶子节点、叶子节点、范围查询、IO

#### 问题42：为什么InnoDB选择B+树而不是B树？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：B+树非叶子节点不存数据，可存储更多键值，树更矮胖，IO更少；叶子节点有序链接，便于范围查询和全表扫描。B树数据分散在各节点，范围查询需中序遍历，效率低。
- **评分关键词**：非叶子节点、矮胖、范围查询、有序链接、IO

#### 问题43：什么是聚簇索引和非聚簇索引？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：聚簇索引叶子节点存完整数据（InnoDB主键索引），非聚簇索引叶子节点存主键值（二级索引）。非聚簇索引查询可能需要回表查主键索引获取完整数据。
- **评分关键词**：聚簇索引、非聚簇索引、叶子节点、回表、主键

#### 问题44：什么情况下索引会失效？
- **难度系数**：0.5
- **追问深度**：场景应用
- **参考答案**：1)使用函数或表达式；2)隐式类型转换；3)like以%开头；4)or条件中有非索引列；5)组合索引不满足最左前缀；6)范围查询后索引失效；7)数据选择性低（如性别）。
- **评分关键词**：函数、类型转换、like、最左前缀、选择性

#### 问题45：MySQL的事务隔离级别有哪些？分别解决什么问题？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：READ_UNCOMMITTED（脏读、不可重复读、幻读）、READ_COMMITTED（不可重复读、幻读）、REPEATABLE_READ（幻读，MySQL默认）、SERIALIZABLE（无问题）。脏读是读到未提交数据，不可重复读是同一事务两次读结果不同，幻读是同一事务两次查询行数不同。
- **评分关键词**：脏读、不可重复读、幻读、MVCC、锁

#### 问题46：MVCC是如何实现的？
- **难度系数**：0.65
- **追问深度**：原理探究
- **参考答案**：通过undo log和read view实现。每行数据有隐藏列DB_TRX_ID（事务ID）、DB_ROLL_PTR（回滚指针）。查询时生成read view，根据事务ID判断数据可见性。可重复读在事务开始时生成read view，读已提交每次查询生成。
- **评分关键词**：undo log、read view、事务ID、回滚指针、可见性

#### 问题47：InnoDB的锁有哪些类型？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：行锁（共享锁S、排他锁X）、间隙锁（锁定索引间隙）、临键锁（行锁+间隙锁）、表锁、意向锁（IS、IX）。行锁通过索引实现，无索引会升级为表锁。
- **评分关键词**：共享锁、排他锁、间隙锁、临键锁、意向锁

#### 问题48：如何优化慢SQL？
- **难度系数**：0.7
- **追问深度**：场景应用
- **参考答案**：1)EXPLAIN分析执行计划；2)添加或优化索引；3)避免SELECT *；4)优化join，小表驱动大表；5)分页优化（延迟关联、覆盖索引）；6)批量操作；7)优化数据类型；8)读写分离。
- **评分关键词**：EXPLAIN、索引、分页优化、延迟关联、批量、读写分离

---

### 4.2 Redis

#### 问题49：Redis的数据类型有哪些？分别适合什么场景？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：String（缓存、计数器）、Hash（对象存储）、List（队列、栈）、Set（去重、交集并集）、Sorted Set（排行榜）、Bitmap（签到、布隆过滤器）、HyperLogLog（UV统计）。
- **评分关键词**：String、Hash、List、Set、SortedSet、Bitmap、场景

#### 问题50：Redis的持久化机制有哪些？
- **难度系数**：0.4
- **追问深度**：原理探究
- **参考答案**：RDB（快照，定时保存，恢复快但可能丢数据）、AOF（记录命令，数据安全但文件大）、混合持久化（RDB+AOF，先全量后增量）。
- **评分关键词**：RDB、AOF、快照、混合持久化、恢复

#### 问题51：什么是缓存穿透、击穿、雪崩？如何解决？
- **难度系数**：0.55
- **追问深度**：场景应用
- **参考答案**：穿透：查询不存在数据，用布隆过滤器或缓存空值；击穿：热点key过期，用互斥锁或逻辑过期；雪崩：大量key同时过期，用随机过期时间、多级缓存、熔断降级。
- **评分关键词**：穿透、击穿、雪崩、布隆过滤器、互斥锁、熔断

#### 问题52：Redis如何实现分布式锁？
- **难度系数**：0.6
- **追问深度**：场景应用
- **参考答案**：SETNX+EXPIRE原子命令设置锁，value用唯一标识（如UUID+线程ID），释放时用Lua脚本判断再删除（保证原子性）。Redisson提供更完善的实现，支持可重入、看门狗续期、红锁。
- **评分关键词**：SETNX、EXPIRE、Lua脚本、Redisson、看门狗、可重入

#### 问题53：Redis集群模式有哪些？
- **难度系数**：0.5
- **追问深度**：原理探究
- **参考答案**：主从复制（读写分离）、哨兵模式（自动故障转移）、Cluster模式（数据分片，16384槽位）。Cluster使用gossip协议通信，客户端缓存槽位映射。
- **评分关键词**：主从复制、哨兵、Cluster、槽位、gossip

---

## 五、分布式系统

### 5.1 分布式事务

#### 问题54：分布式事务的解决方案有哪些？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：2PC/3PC（强一致，性能差）、TCC（业务补偿，侵入性强）、本地消息表（最终一致）、Saga（长事务拆分）、Seata（AT/TCC/Saga/XA模式）。根据业务选择，一般追求最终一致性。
- **评分关键词**：2PC、TCC、Saga、Seata、最终一致、补偿

#### 问题55：2PC有什么问题？
- **难度系数**：0.65
- **追问深度**：原理探究
- **参考答案**：同步阻塞（参与者需等待协调者）、单点故障（协调者挂掉）、数据不一致（协调者发送commit前挂掉，部分提交部分未提交）。3PC通过引入超时机制部分解决，但仍有问题。
- **评分关键词**：同步阻塞、单点故障、数据不一致、超时

#### 问题56：Seata的AT模式是如何工作的？
- **难度系数**：0.7
- **追问深度**：原理探究
- **参考答案**：一阶段：执行业务SQL并记录undo log（前镜像和后镜像）；二阶段成功：异步删除undo log；二阶段失败：根据undo log回滚。通过全局锁保证写隔离，读默认无隔离（可配置）。
- **评分关键词**：undo log、前镜像、后镜像、全局锁、回滚

---

### 5.2 微服务

#### 问题57：服务注册与发现的原理是什么？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：服务启动时向注册中心注册（心跳保活），消费者从注册中心拉取服务列表，通过负载均衡选择实例调用。注册中心集群保证高可用，使用AP（Eureka）或CP（Consul）架构。
- **评分关键词**：注册、心跳、拉取、负载均衡、AP、CP

#### 问题58：熔断降级的原理是什么？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：熔断器有三种状态：CLOSED（正常）、OPEN（熔断，快速失败）、HALF_OPEN（试探）。失败率达到阈值触发熔断，经过休眠时间后进入半开状态，放行少量请求试探，成功则关闭，失败则继续熔断。
- **评分关键词**：CLOSED、OPEN、HALF_OPEN、阈值、休眠时间、试探

#### 问题59：网关的作用是什么？
- **难度系数**：0.4
- **追问深度**：基础概念
- **参考答案**：统一入口、路由转发、负载均衡、认证鉴权、限流熔断、日志监控、协议转换。常用网关有Zuul、Gateway、Kong等。
- **评分关键词**：路由、认证、限流、熔断、统一入口

---

### 5.3 消息队列

#### 问题60：消息队列的作用是什么？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：异步处理（提升响应速度）、应用解耦（系统间不直接依赖）、流量削峰（缓冲高并发请求）、日志处理、消息通讯。
- **评分关键词**：异步、解耦、削峰、缓冲、日志

#### 问题61：Kafka为什么高吞吐？
- **难度系数**：0.6
- **追问深度**：原理探究
- **参考答案**：1)顺序写磁盘（比随机写快）；2)零拷贝（sendfile系统调用）；3)批量处理（批量发送和压缩）；4)分区并行消费；5)页缓存（不直接写磁盘）。
- **评分关键词**：顺序写、零拷贝、批量、分区、页缓存

#### 问题62：如何保证消息的可靠性？
- **难度系数**：0.65
- **追问深度**：场景应用
- **参考答案**：生产者：confirm确认或事务；Broker：消息持久化、副本机制；消费者：手动ACK，业务处理成功后再确认；死信队列处理失败消息。
- **评分关键词**：confirm、持久化、副本、ACK、死信队列

---

## 六、设计模式

#### 问题63：单例模式有哪些实现方式？
- **难度系数**：0.35
- **追问深度**：原理探究
- **参考答案**：饿汉式（类加载时创建，线程安全）、懒汉式（延迟加载，需同步）、双重检查锁（volatile+两次判断）、静态内部类（延迟加载且线程安全）、枚举（最佳方式，防反射和序列化）。
- **评分关键词**：饿汉、懒汉、双重检查、静态内部类、枚举、volatile

#### 问题64：为什么要用双重检查锁？直接synchronized不行吗？
- **难度系数**：0.6
- **追问深度**：原理探究
- **参考答案**：直接synchronized每次获取实例都加锁，性能差。双重检查锁只在第一次创建时加锁，后续直接返回，提升性能。注意要用volatile防止指令重排序导致返回未初始化对象。
- **评分关键词**：性能、指令重排序、volatile、第一次创建

#### 问题65：工厂模式和建造者模式有什么区别？
- **难度系数**：0.45
- **追问深度**：原理探究
- **参考答案**：工厂模式关注创建哪个对象（多态返回），隐藏创建细节；建造者模式关注如何创建复杂对象，分步骤构建，链式调用。工厂用于创建不同类型，建造者用于配置复杂对象。
- **评分关键词**：创建哪个、如何创建、分步骤、链式调用、复杂对象

---

## 七、网络编程

#### 问题66：TCP三次握手的过程是什么？
- **难度系数**：0.3
- **追问深度**：基础概念
- **参考答案**：1)SYN：客户端发送SYN包（seq=x）；2)SYN+ACK：服务端回复SYN+ACK（seq=y, ack=x+1）；3)ACK：客户端发送ACK（ack=y+1），连接建立。三次握手防止历史连接初始化、同步序列号。
- **评分关键词**：SYN、ACK、序列号、历史连接、同步

#### 问题67：为什么TCP要三次握手，两次不行吗？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：两次握手无法防止历史连接（延迟的SYN包导致错误连接），也无法同步双方序列号。三次握手让双方都能确认对方收发能力正常，并协商初始序列号。
- **评分关键词**：历史连接、序列号、收发能力、延迟SYN

#### 问题68：TCP四次挥手的过程是什么？
- **难度系数**：0.35
- **追问深度**：基础概念
- **参考答案**：1)FIN：主动方发送FIN；2)ACK：被动方确认；3)FIN：被动方发送FIN；4)ACK：主动方确认，等待2MSL后关闭。四次是因为被动方可能还有数据要发送，ACK和FIN不能合并。
- **评分关键词**：FIN、ACK、2MSL、数据发送、合并

#### 问题69：为什么主动关闭方要等待2MSL？
- **难度系数**：0.6
- **追问深度**：原理探究
- **参考答案**：MSL是报文最大生存时间。等待2MSL确保：1)最后一个ACK能被对方收到（如果没收到对方会重发FIN）；2)让网络中延迟的报文消失，防止下一个连接收到旧报文。
- **评分关键词**：MSL、ACK、重发、延迟报文、旧连接

#### 问题70：HTTP和HTTPS有什么区别？
- **难度系数**：0.35
- **追问深度**：基础概念
- **参考答案**：HTTPS = HTTP + SSL/TLS。HTTPS通过数字证书验证服务器身份，使用非对称加密交换密钥，对称加密传输数据，保证机密性、完整性、身份认证。
- **评分关键词**：SSL、TLS、证书、加密、身份认证

---

## 八、系统设计与架构

#### 问题71：如何设计一个高并发的秒杀系统？
- **难度系数**：0.75
- **追问深度**：场景应用
- **参考答案**：1)前端：静态化、CDN、验证码、限流；2)网关：限流熔断；3)应用：Redis预减库存、异步下单、消息队列削峰；4)数据库：乐观锁防止超卖；5)兜底：降级策略、排队页面。核心：层层限流、异步处理、最终一致性。
- **评分关键词**：静态化、限流、Redis、消息队列、乐观锁、异步、削峰

#### 问题72：如何设计一个短链系统？
- **难度系数**：0.7
- **追问深度**：场景应用
- **参考答案**：1)发号器生成唯一ID（雪花算法、自增ID）；2)Base62编码转短码；3)存储到数据库和缓存；4)布隆过滤器防止重复；5)302重定向到长链。考虑：读写分离、缓存预热、过期清理。
- **评分关键词**：发号器、Base62、布隆过滤器、302、雪花算法

#### 问题73：如何设计一个分布式ID生成器？
- **难度系数**：0.75
- **追问深度**：场景应用
- **参考答案**：1)数据库自增（简单但性能差）；2)UUID（无序、太长）；3)雪花算法（64位，含时间戳、机器ID、序列号，趋势递增）；4)号段模式（从数据库批量获取ID范围，本地分配）。推荐雪花算法或号段模式。
- **评分关键词**：雪花算法、号段、趋势递增、时间戳、机器ID

#### 问题74：限流算法有哪些？
- **难度系数**：0.55
- **追问深度**：原理探究
- **参考答案**：计数器（简单但临界问题）、滑动窗口（平滑但内存占用大）、令牌桶（匀速处理，可应对突发流量）、漏桶（匀速输出，平滑流量）。令牌桶适合允许突发，漏桶适合严格限速。
- **评分关键词**：计数器、滑动窗口、令牌桶、漏桶、突发流量

#### 问题75：如何排查线上CPU飙高问题？
- **难度系数**：0.7
- **追问深度**：场景应用
- **参考答案**：1)top找到高CPU进程；2)top -Hp找高CPU线程；3)printf '%x'转16进制；4)jstack导出线程栈，grep找nid；5)分析线程栈定位问题代码。常见原因：死循环、频繁GC、正则回溯、大量计算。
- **评分关键词**：top、jstack、线程栈、nid、死循环、GC
