# P2 方案准备记录 — Refs #1

2026-09-17：用户要求依据 Issue，在 master 基础上新建 P2 分支并提供完成方案，解释实现、效果和步骤。

已完成：核实项目绝对路径、相关目录无重解析点、工作区修改、当前 Issue、P1 交接/契约及认证和 API 接入点；获取最新 origin/master；创建 feat/resume-phase2-experience-api，HEAD=d24f5a3，与 master 基线内容相同；保留用户未跟踪提示词。新增本目录三份计划记录，保留 P1 已有计划。

异常：全项目 rg 在旧 P1 测试临时目录收到拒绝访问，未修改权限/清理；git fetch 的 .git/FETCH_HEAD 写入被沙箱拒绝，使用正式审批后成功；创建分支经过正式审批。

本轮业务代码未修改。未执行测试、数据库迁移、PDF 提取、AI 请求、本地提交、push、创建 PR、合并或部署。P2 业务与测试状态均待实现，不能声称完成。

2026-09-17：用户授权按阶段执行。切回 P2 分支并核实允许写入目录。项目环境缺少 FastAPI/httpx/pypdf/JWT 等依赖，新增最小依赖文件；首次 sandbox pip 返回无版本结果，按正式审批流程联网下载二进制 wheel，仅安装项目 .venv。新增可信 Spring-compatible JWT dependency、严格人工输入/API 请求契约、经历分页/JSON 筛选/CRUD/归档/排序/批量路由，以及持久批次/草稿模型和增量迁移。P1 001/002 保持原样。迁移生成首次 Python 命令因 shell 引号失败，未写入文件；改为可审查的 PowerShell 字符串创建新迁移。尚未执行数据库迁移或外部 AI。

2026-09-17：完成持久导入服务、逐页 PDF 提取/AI 草稿适配、修正/删除/确认/重试/取消状态机，原子确认及摘要幂等回执，P1 清理新增来源保护及专用 dry-run 默认维护函数。新增真实 HTTP/JWT/PDF 单元检查和 18 项真实 MySQL/ASGI 业务测试。第一次单元运行因缺少 basetemp 父目录产生 12 个 setup error，缓存写入拒绝；按审批补齐任务专用父目录并执行，74 passed，1.43s。localhost3306 TCP 可连，但未配置测试凭据，未猜测或读取无关密码。已请求安全提供连接。未测试外部 AI、未对旧业务库迁移。

2026-09-17：补充实际 P2 路由认证/显式数据库配置、17 MiB 请求体限额和 AI 流式响应限额（1 MiB）。统一 422 错误结构；人工内容新增空白必填项和 Integer 范围检查；SQL 关键词显式 utf8mb4_bin，避免字段间大小写策略漂移。新增实际 AI HTTP 适配器六项确定性传输验证，以及来源保护/历史快照/部分文件失败三项 MySQL 验收用例（共 21 项 P2 DB 测试）。添加掩码凭据测试入口和完整接口/迁移/回退/幂等交接。全套最终当前可用结果84 passed、52 skipped、4.62s，定向编译及离线 SQL/差异检查通过；跳过均缺少显式 MySQL 测试连接。工具环境 Process/User/Machine 三范围均未提供 URL，未猜测密码。依赖文件暂存检查发现末尾多余空行已修正，未影响行为。创建本地5334893、aad9703阶段提交；下一提交记录测试与文档。未执行实际外部 AI/在线迁移/push/PR/合并/部署。
