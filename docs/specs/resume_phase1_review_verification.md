# P1 review 问题复核

本文前半记录修复前的真实复现，最后一节记录用户授权后的修复验收。当前测试已改为正确行为断言。

复核分支：`feat/resume-phase1-storage-v2`；业务代码仍为 `ab20ac5`。
本轮新增测试和记录，没有修改业务实现、合并或推送。

用户启动测试服务后实际连接到 `127.0.0.1:3306`，版本 MySQL9.6.0。
完整执行：**70 passed，0 skipped，13.12s**。包括原60项测试和10个新复现实例；
新实例中9个使用真实 MySQL，1个是无数据库名称契约检查。
所有数据库操作限夹具新建随机测试 schema，所有被删除文件均为本轮生成的临时文件。
凭据仅掩码交互使用，未写入源码/配置/日志。

**新增测试通过表示观察到其断言的缺陷行为，并不表示业务正确或已经修复。**
修复时需要将这些行为刻画测试改为正确行为的回归断言。

| Review 问题 | 实际证据 | 结论 |
| --- | --- | --- |
| P1：新 FK 与旧 PDF 删除顺序冲突 | 创建真实经历→旧 resumes 引用；直接运行旧 delete_resume 业务体；PDF被移除，MySQL1451阻止记录删除，接口错误码500；回滚后旧记录和经历引用仍在，但PDF不存在 | 已复现 |
| P2：清理批次不能推进 | 创建两条到期记录，limit=1连续执行三次；在首条已经清理和首条被活跃复制件保护两种场景均反复选择首条，第二条文件始终存在、清理标记为空 | 两种情况均已复现 |
| P2：未保护经历来源文件 | 使用正常文件事务创建输出文档和引用同一PDF的经历source_locator；文档到期后dry-run仍列为可删除，实际清理移除PDF，经历记录及来源key仍存在；分别验证未归档和归档经历 | 两种情况均已复现 |
| P2：LaTeX文档缺输出 | 三种输入：不提供文件、只有PDF、只有LaTeX；均关联COMPILED任务并成功提交，format=latex，缺失字段为SQL NULL，记录进入文档列表；代码另确认两字段不可变且无补附接口 | 三种情况均已复现；后续补附受实现限制 |
| P3：名称校验不一致 | DocumentInput接受三个空格；正常创建并提交后数据库仍保存空白名称；rename/copy/save_legacy对相同字符串均抛ValueError | 契约和数据库持久化均已复现 |

## 测试入口与边界

新增 `FastAPI-Backend/tests/test_review_reproductions.py`，复用已有随机MySQL夹具。
原测试使用模拟编译状态/临时字节，本轮仍不声称执行过真实LaTeX编译或AI流程。
存储专用venv没有FastAPI及PDF解析依赖。旧删除测试通过AST加载源码中实际函数业务体，
只移除路由装饰器、类型注解和依赖默认值，避免导入应用配置/启动副作用；使用框架异常替身。
业务语句、MySQL事务、外键和文件删除均实际执行，**没有测试HTTP/ASGI传输层**。

本轮首次连接返回WinError10061/MySQL2003，服务未启动；先执行可用检查得到47 passed、23 skipped。
用户启动服务并要求重试后得到上述70项无跳过结果，不能将首次跳过当作数据库验证。
Python compileall 和 git diff --check 通过。

从项目根目录，安全提供 `RESUME_TEST_MYSQL_URL` 后可运行：

```powershell
$env:PYTHONPATH = Join-Path (Get-Location) 'FastAPI-Backend'
.\.venv\Scripts\python.exe -m pytest FastAPI-Backend/tests --basetemp=.phase1-local/pytest-review-reproductions -q -rP
```

basetemp仅为本任务生成的专用目录，pytest会重新创建它，不得指定用户数据目录。
以上为修复前结论：当时不满足P1合并验收。

## 已授权修复与验收

用户随后要求“尝试修复”。本轮修复五项问题，保持原master基线和当前分支，未合并或推送。

| 问题 | 修复行为 | 回归结果 |
| --- | --- | --- |
| 旧PDF删除回归 | 先提交数据库删除，再删除PDF；外键阻止时返回409并保留PDF；提交不确定或文件删除失败留下清理日志，重试先确认数据库记录不存在 | 引用冲突、正常删除、提交失败、提交确认丢失、文件删除失败后重试通过 |
| 批次无法推进 | 跳过已完成记录；按purge_after/id返回has_more和next_cursor，调用者可推进到被保护首条之后 | limit=1在已清理首条和被保护首条两种情况下均清理第二条 |
| 经历来源文件被清理 | 保护同owner所有经历source_locator中的文件，归档经历同样保留来源 | 未归档/归档来源保留；解除引用后可清理 |
| LaTeX文档缺输出 | 创建强制PDF和LaTeX均存在且验证真实私有文件；新增002迁移数据库CHECK；旧缺失输出行会阻止升级，等待审查过的数据修复 | 三种缺失输入均拒绝并回滚；直接SQL删除任一输出受CHECK阻止；旧异常行升级前检查通过 |
| 名称规则不一致 | 创建、rename、copy、保存历史Markdown共用一个验证器，去首尾空白后校验非空和200字符上限 | 空白输入均拒绝，合法名称统一裁剪 |

清理完成标记改为实际删除后提交；额外验证首个文件删除失败和部分文件删除后失败，均可重试。
002将旧的清理意图标记重置为NULL，保证此前文件删除失败的记录重新检查；实测升级后能清理这些文件。
001冻结迁移没有改写。002只修改resume_documents，未迁移用户现有数据库，也不自动删除旧异常行。

完整运行结果：**78 passed，0 skipped，21.33s**，实际服务MySQL9.6.0。
包括47项单元检查、31项真实数据库实例；原70项中的缺陷断言已改为正确行为断言，另增8项故障/迁移回归。
临时数据库和文件仍仅由夹具创建并清理。旧删除业务体仍使用AST加载，未宣称HTTP/ASGI端到端验证。
本次没有真实LaTeX编译、AI执行或后续阶段的业务接口/页面验证。

本轮修复仅覆盖Phase1及已有PDF删除接口的兼容性，不能据此认定整个issue #1业务完成。
分支保留给用户review；文件清理日志重试与游标循环均是内部维护入口，未启动生产定时任务。
