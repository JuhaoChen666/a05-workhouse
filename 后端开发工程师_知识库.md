# 后端开发工程师知识库

## 一、Java基础

### 1.1 面向对象编程

#### 封装
封装是将数据（属性）和操作数据的方法绑定在一起，并隐藏对象的内部实现细节，只暴露必要的接口。

**核心要点：**
- 使用访问修饰符（private、protected、public）控制访问权限
- 提供getter和setter方法进行数据访问和修改
- 提高代码的安全性和可维护性

#### 继承
继承是子类继承父类的属性和方法，实现代码复用和层次化设计。

**核心要点：**
- 使用`extends`关键字实现继承
- Java只支持单继承，但支持多层继承
- 子类可以重写父类方法（Override）
- 使用`super`关键字调用父类构造方法和方法

#### 多态
多态是指同一操作作用于不同对象产生不同的行为，分为编译时多态（方法重载）和运行时多态（方法重写）。

**核心要点：**
- 方法重载（Overload）：同一类中方法名相同，参数列表不同
- 方法重写（Override）：子类重写父类方法，方法签名相同
- 向上转型：父类引用指向子类对象
- 动态绑定：运行时确定调用哪个方法

### 1.2 集合框架

#### List接口
**ArrayList**
- 基于动态数组实现
- 查询快（O(1)），插入删除慢（O(n)）
- 线程不安全
- 默认初始容量10，扩容为原来的1.5倍

**LinkedList**
- 基于双向链表实现
- 查询慢（O(n)），插入删除快（O(1)）
- 实现了Deque接口，可作队列/栈使用
- 线程不安全

#### Set接口
**HashSet**
- 基于HashMap实现
- 无序，不重复
- 允许null元素
- 非线程安全

**TreeSet**
- 基于红黑树实现
- 有序（自然排序或自定义比较器）
- 不允许null元素
- 非线程安全

**LinkedHashSet**
- 基于LinkedHashMap实现
- 按插入顺序排序
- 性能略低于HashSet

#### Map接口
**HashMap**
- 数组+链表+红黑树（JDK8+）
- 默认初始容量16，负载因子0.75
- 链表长度超过8转为红黑树，低于6转回链表
- 线程不安全

**ConcurrentHashMap**
- 线程安全的HashMap
- JDK7：分段锁（Segment）
- JDK8：CAS+synchronized
- 读操作无锁，写操作细粒度锁

**TreeMap**
- 基于红黑树实现
- 按键的自然顺序或自定义比较器排序

### 1.3 异常处理

#### 异常分类
- **Error**：系统级错误，如OutOfMemoryError、StackOverflowError
- **Exception**：程序可处理的异常
  - **受检异常（Checked Exception）**：编译时必须处理，如IOException
  - **非受检异常（Unchecked Exception）**：运行时异常，如NullPointerException

#### 异常处理机制
- try-catch-finally：捕获和处理异常
- throw：手动抛出异常
- throws：声明方法可能抛出的异常
- try-with-resources：自动关闭资源（AutoCloseable）

### 1.4 多线程与并发

#### 线程创建方式
1. 继承Thread类
2. 实现Runnable接口
3. 实现Callable接口（有返回值）
4. 使用线程池（Executor框架）

#### 线程状态
NEW → RUNNABLE → BLOCKED/WAITING/TIMED_WAITING → TERMINATED

#### 线程同步
- **synchronized**：互斥锁，可修饰方法或代码块
- **ReentrantLock**：可重入锁，支持公平锁、可中断、超时获取
- **ReadWriteLock**：读写锁，读读共享，读写互斥

#### volatile关键字
- 保证可见性：线程修改后对其他线程立即可见
- 禁止指令重排序
- 不保证原子性

#### CAS操作
Compare And Swap，比较并交换
- 使用Unsafe类实现
- 存在ABA问题（可用AtomicStampedReference解决）
- 自旋消耗CPU

#### 线程池
**核心参数：**
- corePoolSize：核心线程数
- maximumPoolSize：最大线程数
- keepAliveTime：非核心线程存活时间
- workQueue：任务等待队列
- threadFactory：线程工厂
- handler：拒绝策略

**拒绝策略：**
- AbortPolicy：直接抛出异常（默认）
- CallerRunsPolicy：由调用线程执行任务
- DiscardPolicy：静默丢弃任务
- DiscardOldestPolicy：丢弃队列最老的任务

**常用线程池：**
- newFixedThreadPool：固定线程数
- newCachedThreadPool：可缓存线程
- newSingleThreadExecutor：单线程
- newScheduledThreadPool：定时任务

---

## 二、JVM

### 2.1 JVM内存结构

#### 堆（Heap）
- 存放对象实例和数组
- 所有线程共享
- 分为新生代（Eden、Survivor0、Survivor1）和老年代
- 垃圾回收的主要区域

#### 方法区（Method Area）
- 存储类信息、常量、静态变量、即时编译器编译后的代码
- JDK8之前：永久代（PermGen）
- JDK8及之后：元空间（Metaspace），使用本地内存

#### 虚拟机栈（VM Stack）
- 每个线程私有
- 存储栈帧（局部变量表、操作数栈、动态链接、方法返回地址）
- 方法调用即入栈，方法结束即出栈

#### 本地方法栈（Native Method Stack）
- 为Native方法服务

#### 程序计数器（Program Counter Register）
- 记录当前线程执行的字节码行号
- 线程私有

#### 直接内存（Direct Memory）
- NIO使用，不受JVM堆大小限制
- 通过DirectByteBuffer操作

### 2.2 垃圾回收

#### 判断对象存活
- **引用计数法**：有循环引用问题
- **可达性分析**：从GC Roots出发，不可达即回收
  - GC Roots：虚拟机栈中引用、方法区静态属性引用、本地方法引用等

#### 引用类型
- **强引用**：不会被回收
- **软引用（SoftReference）**：内存不足时回收
- **弱引用（WeakReference）**：下次GC时回收
- **虚引用（PhantomReference）**：无法获取对象，仅用于回收通知

#### 垃圾回收算法
- **标记-清除**：标记存活对象，清除未标记，产生内存碎片
- **复制算法**：将内存分为两块，复制存活对象，无碎片但浪费空间
- **标记-整理**：标记存活对象，向一端移动，然后清理边界外内存
- **分代收集**：新生代用复制算法，老年代用标记-清除或标记-整理

#### 垃圾收集器
- **Serial**：单线程，新生代，Client模式默认
- **ParNew**：Serial的多线程版本，配合CMS使用
- **Parallel Scavenge**：吞吐量优先，新生代
- **Serial Old**：Serial的老年代版本
- **Parallel Old**：Parallel Scavenge的老年代版本
- **CMS**（Concurrent Mark Sweep）：低停顿，标记-清除算法
  - 初始标记 → 并发标记 → 重新标记 → 并发清除
- **G1**（Garbage First）：分区收集，可预测停顿
  - 将堆划分为多个Region
  - 优先回收价值最大的Region
- **ZGC**：低延迟（<10ms），可处理TB级堆

### 2.3 类加载机制

#### 类加载过程
1. **加载**：读取.class文件，生成Class对象
2. **验证**：验证字节码安全性
3. **准备**：为类变量分配内存并设置默认值
4. **解析**：将符号引用转为直接引用
5. **初始化**：执行类构造器<clinit>()方法

#### 类加载器
- **Bootstrap ClassLoader**：加载JAVA_HOME/lib下的核心类
- **Extension ClassLoader**：加载ext目录下的扩展类
- **Application ClassLoader**：加载classpath下的用户类
- **自定义ClassLoader**：继承ClassLoader实现

#### 双亲委派模型
- 类加载请求先委派给父加载器
- 父加载器无法加载时才自己加载
- 保证核心类库的安全性

---

## 三、Spring框架

### 3.1 IOC容器

#### 核心概念
- **IOC**（Inversion of Control）：控制反转，将对象创建和依赖注入交给容器管理
- **DI**（Dependency Injection）：依赖注入，通过构造器、setter或字段注入依赖

#### Bean生命周期
1. 实例化（构造方法）
2. 属性赋值（依赖注入）
3. 初始化前（BeanNameAware、BeanFactoryAware等）
4. 初始化（InitializingBean.afterPropertiesSet()、@PostConstruct）
5. 初始化后（BeanPostProcessor）
6. 使用
7. 销毁（DisposableBean.destroy()、@PreDestroy）

#### Bean作用域
- **singleton**：单例（默认）
- **prototype**：每次请求创建新实例
- **request**：HTTP请求范围
- **session**：HTTP会话范围
- **application**：ServletContext范围

#### 循环依赖解决
- 构造器注入：无法解决，抛出异常
- setter/字段注入：通过三级缓存解决
  - 一级缓存：成品Bean
  - 二级缓存：早期暴露Bean（已实例化未填充属性）
  - 三级缓存：ObjectFactory，用于生成代理对象

### 3.2 AOP

#### 核心概念
- **Aspect**：切面，横切关注点的模块化
- **Join Point**：连接点，程序执行点（方法调用、异常抛出等）
- **Pointcut**：切点，匹配连接点的表达式
- **Advice**：通知，切面的具体动作
- **Target**：目标对象
- **Proxy**：代理对象

#### 通知类型
- **Before**：方法执行前
- **After**：方法执行后（无论是否异常）
- **AfterReturning**：方法正常返回后
- **AfterThrowing**：方法抛出异常后
- **Around**：环绕通知，可控制方法执行

#### 实现方式
- **JDK动态代理**：目标类实现接口
- **CGLIB代理**：继承目标类，生成子类

#### 事务管理
- **声明式事务**：@Transactional注解
- **传播行为**：REQUIRED、REQUIRES_NEW、NESTED等
- **隔离级别**：READ_UNCOMMITTED、READ_COMMITTED、REPEATABLE_READ、SERIALIZABLE
- **回滚规则**：默认RuntimeException回滚，可配置

### 3.3 Spring Boot

#### 自动配置原理
- @EnableAutoConfiguration启用自动配置
- spring.factories中定义自动配置类
- @Conditional条件注解控制配置生效
- 通过xxxProperties绑定配置文件

#### Starter机制
- 预定义的依赖组合
- 自动配置相关组件
- 约定大于配置

#### 内嵌服务器
- Tomcat（默认）
- Jetty
- Undertow

---

## 四、数据库

### 4.1 MySQL

#### 索引
**B+树索引**
- 非叶子节点只存储键值和指针
- 叶子节点存储完整数据，且按顺序链接
- 支持范围查询和顺序访问

**索引类型**
- 主键索引：唯一非空
- 唯一索引：唯一可为空
- 普通索引：无限制
- 组合索引：多列索引，遵循最左前缀原则
- 全文索引：文本搜索

**索引优化**
- 选择性高的列建索引
- 避免在索引列上使用函数
- 避免隐式类型转换
- 覆盖索引减少回表

#### 事务
**ACID特性**
- Atomicity（原子性）：要么全成功，要么全失败
- Consistency（一致性）：数据状态一致
- Isolation（隔离性）：事务间相互隔离
- Durability（持久性）：提交后永久保存

**隔离级别及问题**
- READ_UNCOMMITTED：脏读、不可重复读、幻读
- READ_COMMITTED：不可重复读、幻读
- REPEATABLE_READ：幻读（MySQL默认，MVCC解决）
- SERIALIZABLE：无问题，性能最差

**MVCC**
- 多版本并发控制
- 通过undo log和read view实现
- 每行数据有隐藏列：DB_TRX_ID、DB_ROLL_PTR、DB_ROW_ID

#### 锁机制
- **行锁**：InnoDB支持，粒度小，并发高
  - 共享锁（S锁）：读锁
  - 排他锁（X锁）：写锁
- **表锁**：MyISAM使用，粒度大，并发低
- **间隙锁**：锁定索引记录间隙，防止幻读
- **临键锁**：行锁+间隙锁

#### SQL优化
- 避免SELECT *
- 使用EXPLAIN分析执行计划
- 合理使用索引
- 避免全表扫描
- 分页优化（延迟关联、覆盖索引）
- 批量操作减少交互

### 4.2 Redis

#### 数据类型
- **String**：字符串、整数、浮点数
- **Hash**：键值对集合
- **List**：双向链表，可作队列/栈
- **Set**：无序不重复集合
- **Sorted Set**：有序集合，带score
- **Bitmap**：位图
- **HyperLogLog**：基数统计
- **Geo**：地理位置

#### 持久化
**RDB**
- 快照方式，定时保存内存数据到磁盘
- 文件紧凑，恢复速度快
- 可能丢失最后一次快照后的数据

**AOF**
- 记录写操作命令
- 数据安全性高
- 文件大，恢复慢
- 可配置always/everysec/no

**混合持久化**
- RDB+AOF
- 先RDB全量，再AOF增量

#### 缓存问题
**缓存穿透**
- 查询不存在的数据，直接打到数据库
- 解决：布隆过滤器、缓存空值

**缓存击穿**
- 热点key过期，大量请求打到数据库
- 解决：互斥锁、逻辑过期

**缓存雪崩**
- 大量key同时过期
- 解决：随机过期时间、多级缓存、熔断降级

#### 分布式锁
- SETNX + EXPIRE
- Redisson：可重入锁、看门狗续期、红锁

#### 集群模式
- **主从复制**：读写分离，数据冗余
- **哨兵模式**：自动故障转移
- **Cluster模式**：数据分片，16384个槽位

---

## 五、分布式系统

### 5.1 分布式事务

#### 2PC（两阶段提交）
- 准备阶段：协调者询问参与者是否可执行
- 提交阶段：所有参与者同意则提交，否则回滚
- 缺点：同步阻塞、单点故障、数据不一致风险

#### 3PC（三阶段提交）
- CanCommit、PreCommit、DoCommit
- 增加预提交阶段，减少阻塞时间
- 引入超时机制

#### TCC
- Try：预留资源
- Confirm：确认执行业务
- Cancel：取消释放资源
- 业务侵入性强，需实现三个接口

#### 本地消息表
- 将分布式事务转为本地事务
- 通过消息队列异步通知
- 最终一致性

#### Seata
- AT模式：自动补偿，类似2PC
- TCC模式：手动实现TCC
- Saga模式：长事务拆分，按正向/逆向补偿
- XA模式：基于XA协议的2PC

### 5.2 微服务

#### 服务注册与发现
- **Eureka**：AP架构，自我保护机制
- **Nacos**：支持配置管理，CP/AP可切换
- **Consul**：支持健康检查，CP架构

#### 负载均衡
- **Ribbon**：客户端负载均衡
- 轮询、随机、加权、最少连接等策略

#### 服务调用
- **Feign**：声明式HTTP客户端
- **Dubbo**：RPC框架，高性能

#### 服务熔断降级
- **Hystrix**：熔断、降级、限流
  - 熔断器状态：CLOSED、OPEN、HALF_OPEN
- **Sentinel**：阿里开源，功能更丰富

#### 网关
- **Zuul**：Netflix开源，基于Servlet
- **Gateway**：Spring官方，基于WebFlux，性能更好
- 功能：路由、过滤、限流、认证

#### 配置中心
- **Spring Cloud Config**：Git存储
- **Nacos**：支持动态刷新
- **Apollo**：携程开源，功能完善

### 5.3 消息队列

#### Kafka
- 高吞吐、分布式、持久化
- Topic分区，支持水平扩展
- 消费者组实现负载均衡
- 副本机制保证高可用

#### RocketMQ
- 阿里开源，低延迟、高可靠
- 支持顺序消息、事务消息、延时消息
- NameServer管理路由信息

#### RabbitMQ
- 基于AMQP协议
- 支持多种交换机类型（direct、topic、fanout、headers）
- 消息可靠性高

#### 消息可靠性
- 生产者确认（confirm/transaction）
- 消息持久化
- 消费者ACK确认
- 死信队列处理失败消息

---

## 六、设计模式

### 6.1 创建型模式

#### 单例模式
- 确保一个类只有一个实例
- 饿汉式、懒汉式、双重检查锁、静态内部类、枚举

#### 工厂模式
- 简单工厂：一个工厂类创建所有产品
- 工厂方法：每个产品有对应工厂
- 抽象工厂：创建产品族

#### 建造者模式
- 分步骤构建复杂对象
- 链式调用，可读性好

### 6.2 结构型模式

#### 代理模式
- 静态代理：手动编写代理类
- 动态代理：JDK动态代理、CGLIB代理

#### 装饰器模式
- 动态给对象添加职责
- 包装器模式

#### 适配器模式
- 将不兼容接口转为兼容接口
- 类适配器、对象适配器

### 6.3 行为型模式

#### 观察者模式
- 一对多依赖关系
- 主题状态变化通知所有观察者

#### 策略模式
- 定义算法族，分别封装
- 运行时切换算法

#### 模板方法模式
- 定义算法骨架，子类实现具体步骤

---

## 七、网络编程

### 7.1 HTTP/HTTPS

#### HTTP状态码
- 1xx：信息响应
- 2xx：成功（200 OK、201 Created、204 No Content）
- 3xx：重定向（301 Moved Permanently、302 Found、304 Not Modified）
- 4xx：客户端错误（400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found）
- 5xx：服务器错误（500 Internal Server Error、502 Bad Gateway、503 Service Unavailable）

#### HTTP/1.1 vs HTTP/2
- HTTP/1.1：串行请求、队头阻塞、文本格式
- HTTP/2：多路复用、二进制分帧、头部压缩、服务器推送

#### HTTPS
- HTTP + SSL/TLS
- 对称加密传输数据
- 非对称加密交换密钥
- 数字证书验证身份

### 7.2 TCP/IP

#### TCP三次握手
1. SYN：客户端发送SYN包
2. SYN+ACK：服务端回复SYN+ACK
3. ACK：客户端发送ACK，连接建立

#### TCP四次挥手
1. FIN：主动方发送FIN
2. ACK：被动方确认
3. FIN：被动方发送FIN
4. ACK：主动方确认，等待2MSL后关闭

#### TCP与UDP
- TCP：面向连接、可靠、有序、流量控制、拥塞控制
- UDP：无连接、不可靠、无序、开销小、速度快

---

## 八、常用工具与框架

### 8.1 Git

#### 常用命令
- git clone：克隆仓库
- git add：添加文件到暂存区
- git commit：提交更改
- git push：推送到远程
- git pull：拉取并合并
- git branch：分支管理
- git merge：合并分支
- git rebase：变基

#### 分支策略
- Git Flow：master、develop、feature、release、hotfix
- GitHub Flow：简单，feature分支合并到main
- GitLab Flow：结合环境分支

### 8.2 Maven/Gradle

#### Maven生命周期
- clean：清理
- default：编译、测试、打包、安装、部署
- site：生成站点文档

#### 依赖管理
- 依赖传递
- 依赖冲突：就近原则、先声明优先
- 排除依赖、依赖范围（compile、test、provided、runtime）

### 8.3 Docker

#### 核心概念
- 镜像（Image）：只读模板
- 容器（Container）：镜像的运行实例
- 仓库（Repository）：存储镜像

#### 常用命令
- docker build：构建镜像
- docker run：运行容器
- docker ps：查看运行中容器
- docker exec：进入容器
- docker logs：查看日志

#### Dockerfile指令
- FROM：基础镜像
- RUN：执行命令
- COPY/ADD：复制文件
- CMD/ENTRYPOINT：容器启动命令
- EXPOSE：暴露端口

---

## 九、系统设计与架构

### 9.1 高并发设计

#### 限流
- 计数器算法
- 滑动窗口
- 令牌桶
- 漏桶

#### 降级
- 开关降级
- 自动降级
- 兜底数据

#### 熔断
- 失败率达到阈值触发熔断
- 熔断后快速失败
- 半开状态试探恢复

### 9.2 高可用设计

#### 负载均衡
- DNS负载均衡
- 硬件负载均衡（F5）
- 软件负载均衡（Nginx、LVS、HAProxy）

#### 主备切换
- 主从复制
- 哨兵监控
- 自动故障转移

#### 数据一致性
- 强一致性：同步复制
- 最终一致性：异步复制+补偿

### 9.3 性能优化

#### 数据库优化
- SQL优化
- 索引优化
- 分库分表
- 读写分离
- 缓存优化

#### 应用优化
- 异步处理
- 批量操作
- 连接池
- 本地缓存
- JVM调优

#### 系统优化
- CDN加速
- 静态资源分离
- 压缩传输
- 长连接复用
