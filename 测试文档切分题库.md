## 一、Java基础语法

### 1. Java中基本数据类型有哪些？

**答案**：Java有8种基本数据类型，分为4类：

- 整数类型：byte（1字节）、short（2字节）、int（4字节）、long（8字节）
- 浮点类型：float（4字节）、double（8字节）
- 字符类型：char（2字节）
- 布尔类型：boolean（理论上1位，实际实现中通常是1字节）

**考点延伸**：每种类型的默认值、包装类、自动装箱拆箱机制。

------

### 2. 请解释自动装箱和拆箱

**答案**：

- **自动装箱**：将基本数据类型自动转换为对应的包装类对象

  java

  ```
  int i = 10;
  Integer integer = i;  // 自动装箱
  ```

  

- **自动拆箱**：将包装类对象自动转换为对应的基本数据类型

  java

  ```
  Integer integer = 20;
  int j = integer;  // 自动拆箱
  ```

  

------

### 3. String、StringBuilder和StringBuffer的区别

**答案**：

| 特性     | String                   | StringBuilder | StringBuffer             |
| :------- | :----------------------- | :------------ | :----------------------- |
| 可变性   | 不可变                   | 可变          | 可变                     |
| 线程安全 | 线程安全（不可变）       | 非线程安全    | 线程安全（synchronized） |
| 性能     | 低（频繁操作创建新对象） | 高            | 中                       |

**应用场景**：

- String：操作少量数据或字符串常量
- StringBuilder：单线程环境下大量字符串拼接
- StringBuffer：多线程环境下大量字符串拼接

------

## 二、面向对象编程

### 4. 面向对象的四大特性是什么？

**答案**：

1. **封装**：将数据和操作数据的方法绑定在一起，隐藏内部实现细节，对外提供必要接口

   java

   ```
   public class User {
       private String name;  // 私有变量
       
       public String getName() { return name; }  // 公共访问方法
       public void setName(String name) { this.name = name; }
   }
   ```

   

2. **继承**：子类继承父类的属性和方法，使用`extends`关键字

   java

   ```
   class Animal { void eat() { } }
   class Dog extends Animal { void bark() { } }  // Dog继承Animal
   ```

   

3. **多态**：同一方法调用根据不同对象类型表现不同行为（方法重写实现）

   java

   ```
   interface Animal { void sound(); }
   class Dog implements Animal { 
       @Override public void sound() { System.out.println("Bark"); }
   }
   ```

   

4. **抽象**：隐藏实现细节，只提供功能定义（抽象类/接口）

------

### 5. 方法重载（Overloading）与方法重写（Overriding）的区别

**答案**：

| 特性       | 方法重载       | 方法重写           |
| :--------- | :------------- | :----------------- |
| 发生位置   | 同一个类中     | 子类与父类之间     |
| 方法名     | 相同           | 相同               |
| 参数列表   | 必须不同       | 必须相同           |
| 返回值     | 可以不同       | 必须相同（或子类） |
| 访问修饰符 | 无限制         | 不能严于父类       |
| 绑定时机   | 编译期静态绑定 | 运行期动态绑定     |

**重载示例**：

java

```
public class Calculator {
    public int add(int a, int b) { return a + b; }
    public double add(double a, double b) { return a + b; }
    public int add(int a, int b, int c) { return a + b + c; }
}
```



**重写示例**：

java

```
class Animal { public void makeSound() { System.out.println("Animal sound"); } }
class Dog extends Animal { 
    @Override public void makeSound() { System.out.println("Dog barks"); }
}
```



------

### 6. 什么是构造方法？与普通方法的区别？

**答案**：

**构造方法特点**：

- 方法名与类名相同
- 没有返回值类型（连void都没有）
- 使用`new`关键字时自动调用
- 不能被继承、重写，但可以重载

**与普通方法的区别**：

- 构造方法用于初始化对象，普通方法用于定义对象行为
- 构造方法在对象创建时自动执行，普通方法需要显式调用

------

## 三、集合框架

### 7. ArrayList与LinkedList的区别

**答案**：

| 特性      | ArrayList                | LinkedList             |
| :-------- | :----------------------- | :--------------------- |
| 底层实现  | 动态数组                 | 双向链表               |
| 随机访问  | 高效（O(1)）             | 低效（O(n)需遍历）     |
| 插入/删除 | 中间位置慢（需移动元素） | 头尾快（O(1)），中间慢 |
| 内存占用  | 较少（只存数据）         | 较多（存储前后指针）   |
| 适用场景  | 频繁查询、少量修改       | 频繁插入删除           |

**扩容机制**：ArrayList初始容量10，扩容为1.5倍。

------

### 8. HashMap的工作原理

**答案**：

- **底层结构**：数组+链表/红黑树（JDK 8+）
- **初始容量**：16
- **负载因子**：0.75
- **扩容机制**：容量翻倍 + 重新哈希
- **put流程**：计算key的hash值 → 定位数组索引 → 处理hash冲突（链表/树化）
- **线程安全**：非线程安全，多线程环境下使用ConcurrentHashMap

------

## 四、异常处理

### 9. 简述Java异常体系

**答案**：

**两大类异常**：

1. **Checked Exception（受检异常）**
   - 编译时必须处理（try-catch或throws）
   - 如：IOException、SQLException、FileNotFoundException
2. **Unchecked Exception（非受检异常，运行时异常）**
   - 编译时不强制处理
   - 如：NullPointerException、ArrayIndexOutOfBoundsException、ArithmeticException

------

### 10. try-catch-finally的执行顺序

**答案**：

**关键规则**：

- finally块总会在return之前执行（除非调用System.exit()）
- 如果finally中有return，会覆盖try/catch中的return

**示例1**：

java

```
public static int test1() {
    int a = 1;
    try {
        System.out.println(a / 0);  // 抛出异常
        a = 2;
    } catch (ArithmeticException e) {
        a = 3;
        return a;  // 返回前先执行finally
    } finally {
        a = 4;  // 执行但不影响返回值
    }
    return a;
}
// 返回 3
```



**示例2**：

java

```
public static int test2() {
    int a = 1;
    try {
        System.out.println(a / 0);
    } catch (ArithmeticException e) {
        a = 3;
        return a;
    } finally {
        a = 4;
        return a;  // 覆盖catch中的return
    }
}
// 返回 4
```



------

## 五、多线程与并发

### 11. 创建线程的三种方式

**答案**：

**方式一：继承Thread类**

java

```
class MyThread extends Thread {
    @Override public void run() {
        System.out.println("Thread running");
    }
}
new MyThread().start();
```



**方式二：实现Runnable接口**

java

```
class MyRunnable implements Runnable {
    @Override public void run() {
        System.out.println("Runnable running");
    }
}
new Thread(new MyRunnable()).start();
```



**方式三：实现Callable接口（有返回值）**

java

```
class MyCallable implements Callable<String> {
    @Override public String call() {
        return "Task completed";
    }
}
FutureTask<String> task = new FutureTask<>(new MyCallable());
new Thread(task).start();
String result = task.get();  // 获取返回值
```



------

### 12. 什么是线程安全？如何实现？

**答案**：

**线程安全问题**：多个线程同时访问共享资源导致数据不一致。

**解决方式**：

1. **synchronized关键字**

   java

   ```
   public synchronized void safeMethod() {
       // 临界区代码
   }
   ```

   

2. **Lock接口**

   java

   ```
   Lock lock = new ReentrantLock();
   lock.lock();
   try {
       // 临界区代码
   } finally {
       lock.unlock();
   }
   ```

   

3. **原子类**

   java

   ```
   AtomicInteger count = new AtomicInteger();
   count.incrementAndGet();  // 原子操作
   ```

   

4. **volatile关键字**：保证可见性，但不保证原子性

------

### 13. 简述synchronized和Lock的区别

**答案**：

| 特性       | synchronized            | Lock                            |
| :--------- | :---------------------- | :------------------------------ |
| 用法       | 关键字，修饰方法/代码块 | 接口，需显式调用lock()/unlock() |
| 锁获取     | 自动获取和释放          | 需手动释放（finally中）         |
| 可中断     | 不支持                  | 支持lockInterruptibly()         |
| 公平锁     | 非公平                  | 可设置公平性                    |
| 尝试获取锁 | 不支持                  | 支持tryLock()                   |
| 性能       | JDK6后优化              | 灵活度更高                      |

------

## 六、JVM基础

### 14. JVM内存区域划分

**答案**：

- **堆（Heap）**：存放对象实例，所有线程共享
  - 新生代（Eden、Survivor）
  - 老年代
- **方法区（Metaspace）**：类信息、常量池（JDK8后元空间）
- **虚拟机栈（Stack）**：线程私有，存储局部变量表、操作数栈
- **本地方法栈**：为native方法服务
- **程序计数器**：线程私有，记录当前执行位置

------

### 15. 双亲委派模型

**答案**：

**作用**：

- 避免类重复加载
- 保证核心类库安全（防止自定义java.lang.String）

**工作流程**：类加载器收到加载请求 → 先委托父加载器尝试加载 → 父加载器无法加载时才自己加载

------

## 七、代码示例（用于测试）

### 示例1：单例模式（双重检查锁）

java

```
public class Singleton {
    private static volatile Singleton instance;
    
    private Singleton() {}
    
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```



### 示例2：遍历目录下所有文件（递归）

java

```
public static void getAllFilePaths(File filePath, List<String> paths) {
    File[] files = filePath.listFiles();
    if (files == null) return;
    
    for (File f : files) {
        if (f.isDirectory()) {
            paths.add(f.getPath());
            getAllFilePaths(f, paths);
        } else {
            paths.add(f.getPath());
        }
    }
}
```